version: 0.0
os: linux
files:
  - source: /
    destination: /home/c2-user/nube/Mariangela/prisma-back
    overwrite: true
hooks:
  ApplicationStart:
    - location: script/flask.sh
      timeout: 3600
